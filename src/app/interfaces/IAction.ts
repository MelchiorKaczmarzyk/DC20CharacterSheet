export interface IAction {
    apCost: number,
    spCost: number,
    mpCosst: number,
    source: string, //class (which?), ancestry (which?), talent (which? - np multiclass)
    range: string,
    checks: string[],

}